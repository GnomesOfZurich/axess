# Copilot Adapter

Canonical repository guidance lives in [`../AGENTS.md`](../AGENTS.md).

Use that file as the source of truth for project boundaries, repository map, coding rules, naming conventions, architecture, security principles, feature flags, and testing expectations.

Copilot-specific note:

- Keep this file thin. If project guidance changes, update `AGENTS.md` first instead of duplicating full repo context here.
- Tool-specific deltas (e.g. Copilot-only formatting hints, autocomplete cues) belong here, not in `AGENTS.md`.
